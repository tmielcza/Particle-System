#version 400

uniform vec4 gcenter;
in vec4 VertexPos;
out vec4 color;

void main () {
	vec3 color1 = vec3(1.0, 0.0, 0.0);
	vec3 color2 = vec3(1.0, 1.0, 1.0);
	color = vec4(mix(color1, color2, 1 / pow(length(VertexPos - gcenter) * 20.0, 0.5)), 0.2f);
}
